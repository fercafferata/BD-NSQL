<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Locales</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
</head>
<body>
  <header>
    <ul class="nav nav-pills nav-fill">
      <li class="nav-item">
        <a class="nav-link" href="/">Inicio</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/cargar">Cargar</a>
      </li>
      <li class="nav-item">
        <a class="nav-link active" aria-current="page" href="#">Locales</a>
      </li>
    </ul>
  </header>
  
  <div class="container mt-4">
    <form class="form-inline" method="GET" action="/location5km">
        <div class="mb-3">
            <label for="latitud" class="form-label">Latitud: (ejemplo: -32.47985528838145)</label>
            <input type="double" class="form-control" name="latitud">
        </div>
        <div class="mb-3">
            <label for="longitud" class="form-label">Longitud: (ejemplo: -58.23535741670090)</label>
            <input type="double" class="form-control" name="longitud">
        </div>

        <button type="submit" class="btn btn-primary">Cargar</button>
    </form>

    <div class="mt-4">
      <h3>Cervecerias:</h3>
      {{#each cervecerias}}
        <div class="accordion mb-2" id="accordionCervecerias{{@index}}">
          <div class="accordion-item">
            <h2 class="accordion-header" id="headingCerveceria{{@index}}">
              <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapseCerveceria{{@index}}" aria-expanded="false" aria-controls="collapseCerveceria{{@index}}">
                {{this.[0]}}
              </button>
            </h2>
            <div id="collapseCerveceria{{@index}}" class="accordion-collapse collapse" aria-labelledby="headingCerveceria{{@index}}" data-bs-parent="#accordionCervecerias{{@index}}">
              <div class="accordion-body">
                {{this.[1]}} km
              </div>
            </div>
          </div>
        </div>
      {{/each}}

      <h3>Universidades:</h3>
      {{#each universidades}}
        <div class="accordion mb-2" id="accordionUniversidades{{@index}}">
          <div class="accordion-item">
            <h2 class="accordion-header" id="headingUniversidad{{@index}}">
              <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapseUniversidad{{@index}}" aria-expanded="false" aria-controls="collapseUniversidad{{@index}}">
                {{this.[0]}}
              </button>
            </h2>
            <div id="collapseUniversidad{{@index}}" class="accordion-collapse collapse" aria-labelledby="headingUniversidad{{@index}}" data-bs-parent="#accordionUniversidades{{@index}}">
              <div class="accordion-body">
                {{this.[1]}} km
              </div>
            </div>
          </div>
        </div>
      {{/each}}
      
      <h3>Farmacias:</h3>
      {{#each farmacias}}
        <div class="accordion mb-2" id="accordionFarmacias{{@index}}">
          <div class="accordion-item">
            <h2 class="accordion-header" id="headingFarmacia{{@index}}">
              <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapseFarmacia{{@index}}" aria-expanded="false" aria-controls="collapseFarmacia{{@index}}">
                {{this.[0]}}
              </button>
            </h2>
            <div id="collapseFarmacia{{@index}}" class="accordion-collapse collapse" aria-labelledby="headingFarmacia{{@index}}" data-bs-parent="#accordionFarmacias{{@index}}">
              <div class="accordion-body">
                {{this.[1]}} km
              </div>
            </div>
          </div>
        </div>
      {{/each}}
      
      <h3>Emergencias:</h3>
      {{#each emergencias}}
        <div class="accordion mb-2" id="accordionEmergencias{{@index}}">
          <div class="accordion-item">
            <h2 class="accordion-header" id="headingEmergencia{{@index}}">
              <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapseEmergencia{{@index}}" aria-expanded="false" aria-controls="collapseEmergencia{{@index}}">
                {{this.[0]}}
              </button>
            </h2>
            <div id="collapseEmergencia{{@index}}" class="accordion-collapse collapse" aria-labelledby="headingEmergencia{{@index}}" data-bs-parent="#accordionEmergencias{{@index}}">
              <div class="accordion-body">
                {{this.[1]}} km
              </div>
            </div>
          </div>
        </div>
      {{/each}}
      
      <h3>Supermercados:</h3>
      {{#each supermercados}}
        <div class="accordion mb-2" id="accordionSupermercados{{@index}}">
          <div class="accordion-item">
            <h2 class="accordion-header" id="headingSupermercado{{@index}}">
              <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapseSupermercado{{@index}}" aria-expanded="false" aria-controls="collapseSupermercado{{@index}}">
                {{this.[0]}}
              </button>
            </h2>
            <div id="collapseSupermercado{{@index}}" class="accordion-collapse collapse" aria-labelledby="headingSupermercado{{@index}}" data-bs-parent="#accordionSupermercados{{@index}}">
              <div class="accordion-body">
                {{this.[1]}} km
              </div>
            </div>
          </div>
        </div>
      {{/each}}
    </div>
  </div>

  <!-- Option 1: Bootstrap Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>
</html>